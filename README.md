# TicTacToe

Betclic interview application.

## Getting Started

### Using FVM

```bash
fvm flutter clean
fvm flutter pub get
fvm flutter pub run build_runner build --delete-conflicting-outputs
```

### Wtihout FVM - Flutter 3.38.1 required

```bash
flutter clean
flutter pub get
flutter pub run build_runner build --delete-conflicting-outputs
```
